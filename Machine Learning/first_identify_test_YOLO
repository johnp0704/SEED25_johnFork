'''Very simple binary classification example; given 100 annotated training images (dandelion wave 1), and given 1 new test image, classify if the image
has a dandelion. Does not worry about where or how many'''

#Make sure to install ultralytics
from ultralytics import YOLO

# Load a pretrained YOLO model (small one is fine to start)
model = YOLO("yolov8n.pt")  # or "yolo11n.pt" if you have YOLOv11

# Train it on your data
model.train(
    data=r"C:\UVM\SEED\SEED25_johnFork\Machine Learning\dandelion.yaml",
    epochs=10,
    imgsz=640,
    batch=8,
)

